# 금융한걸음 — 팀 공통 규칙

> **이 파일은 통합자만 고친다.**
> 기능을 추가한다고 여기에 줄을 더하지 않는다.
> 내 작업에만 해당하는 규칙은 **내 폴더의 `CLAUDE.md`** 에 쓴다.

## 이 프로젝트

고령자가 가짜 은행 화면에서 송금을 직접 연습하는 웹 학습 서비스. 실제 돈은 오가지 않는다.
AI는 대신 눌러주지 않고 설명·안내·피드백만 한다. 3명 · 1일 · MVP.

- 실행: `python3 -m http.server 8000` 후 `http://localhost:8000/screen/bank-ui.html`
- **`file://` 로 열지 않는다.** `fetch` 로 데이터를 읽으므로 열리지 않는다
- 검증: `OWNERS.md` 의 「검증 명령」 3단계 (테스트 러너 없음 — 눈 확인 + 콘솔 확인)
- 빌드·번들러·프레임워크 없음. 바닐라 JS, 모듈 없이 `window.*` 전역
- 저장소: `https://github.com/MiriKim79/bank_simulator.git` — 흐름은 `feature/* → develop → main`. **각자 자기 `feature/*` 브랜치에서 커밋하고 PR을 `develop` 대상으로 연다.** `develop`·`main` 직접 커밋은 원칙적으로 금지 — 공통 계약(`shared/CONTRACT.md`) 변경처럼 셋이 같이 고쳐야 하는 경우만 합의 후 예외로 `develop`에 직접 커밋한다. `main`은 `develop`을 머지해서만 올라간다(배포). 규칙은 `ZIP-PROTOCOL.md` 「Git 협업 규칙」
- **미션은 송금 1개다** — 김민수에게 5만원. 보이스피싱·잔액조회·거래내역은 이번 MVP 제외 (`SPEC.md` 4.3)
- **UI/UX 레퍼런스**: 신한 SOL뱅크 「돈 보내기」 체험 (`SPEC.md` 13절). 단계 흐름·정보 구조·입력 방식·버튼 배치를 참고해 **비슷한 경험**을 주되, **로고·브랜드명·색상·이미지·아이콘은 복제하지 않는다.** 화면은 가상 은행이다

## 작업 시작 규칙 (중요)

**파일을 고치기 전에, 그 폴더의 `CLAUDE.md` 를 먼저 읽는다.**

폴더마다 자기 규칙이 따로 있다. 안 읽고 고치면 팀 약속을 어긴다.

무엇을 만들지는 **`SPEC.md` → `plan.md` → `tasks.md`** 순서로 본다.
`SPEC.md` 가 근거이고, 셋이 어긋나면 `SPEC.md` 가 이긴다.

**범위는 3명·1일 MVP다. 거기 없는 기능을 더하지 않는다.**
쓰다 보니 SPEC과 어긋나는 게 나오면 **조용히 고치지 말고 그 자리에 표시한다.** 고치는 것은 사람이 정한다.
레퍼런스(SOL)와 SPEC이 다를 때도 같다 — 임의로 맞추지 않고 **반영 / 단순화 / 제외 / 팀 결정 필요** 로 분류해 `SPEC.md` 13.3에 적는다.

**팀 결정 대기 5건은 2026-08-19에 확정됐다 (`SPEC.md` 12절).** TTS(F11)는 Should로 상향, 퀴즈 오답 처리는 "설명 후 다음 문제"로 확정, 은행명은 가상 은행(`한걸음은행`)으로 확정, 비밀번호 단계는 이번 MVP 제외로 확정, 목표 금액은 홈 화면에 한 번만 표시로 확정. 자세한 확정값은 `SPEC.md` 12절을 본다.

## 소유권

누가 어느 폴더를 소유하는지는 `OWNERS.md` 에 있다. **작업 전에 읽는다.**

| 폴더 | 레인 | 담당 |
|---|---|---|
| `screen/` | A 화면 | 영원 |
| `mission/` | B 로직 | 서정 |
| `guide/` | C 답변·음성 | 미리 (통합자 겸임) |
| `shared/` | 통합자 | 미리 |

- 내 폴더 밖은 **읽기만** 한다
- 뚫어야 한다면 `CHANGELOG-INBOX/` 에 쪽지를 남기고 카톡으로 통보한다
- 담당·통합자는 위 표대로 확정됐다. 각자 `OWNERS.md` 의 「담당이 정해지면 할 일」 3단계(deny·확인·훅)를 실행한다

## 공통 약속

`OWNERS.md` 의 「공통 약속」 표를 **그대로** 따른다. 확정본은 `shared/CONTRACT.md`.
함수 이름·데이터 모양을 바꿔야 하면 **고치기 전에 사람에게 물어본다.**

특히 이 셋은 말없이 바꾸면 팀 전체가 멈춘다.

- `AIGuide.askAI(question, currentStep, mission)` — **인자 3개.** 2개로 줄이면 미션 내용이 두 곳에 생긴다
- `mission/missions.json` 의 단계 스키마 — `label` 을 지우면 진행 표시(F17)가 빈칸이 된다
- 버튼 `data-action` 값 — 화면과 판정이 이 문자열로만 만난다

미션 내용(단계·정답·오답 문구·단계 이름)은 **`mission/missions.json` 에만** 있다. JS에 하드코딩하지 않는다.

## 하지 말 것

- 이 파일(`CLAUDE.md`)과 `OWNERS.md` 수정 — 통합자만
- `main` 에 직접 커밋 — `main` 은 `develop` 을 머지해서만 올라간다
- `README.md`·`.gitignore`·`.github/**`·`docs/research/**` 수정 — 원격에서 온 팀 파일이다. 고칠 게 있으면 PR 로 제안한다
- `shared/` 수정 — 통합자만
- 새 라이브러리·빌드 도구 추가 — 먼저 사람에게 물어본다
- **Won't 기능 구현** — F9(반복 오답 힌트 상승), F10(단계별 안내 배너). Must 레인에서 `speechSynthesis` 호출 금지(F11은 Should로 올라갔지만 Must 완료 전까지는 착수하지 않는다)
  - 작은 진행 표시(F17, `2 / 5 단계 — 계좌번호 입력`)는 F10이 **아니다.** 그건 만든다
- API 키·토큰을 소스에 넣기 — 정적 페이지라 소스에 그대로 노출된다. LLM 설정은 `shared/llm-config.js`(커밋 안 함)
- **실제 은행 브랜드 복제** — 로고·브랜드명·고유 색상·아이콘. 레퍼런스는 구조만 본다. 화면 안 은행명은 확정된 가상 은행명(`한걸음은행`/`새봄은행`/`푸른은행`)만 쓴다
- **SPEC에 없는 기능 추가** — 미션 추가, 메모, 수수료, 다음 미션 이어가기, 6자리 비밀번호 단계 등. 필요해 보이면 만들지 말고 `SPEC.md` 12절에 결정 항목으로 올린다
- **`main`·`develop` 직접 기능 개발** — 기능 개발은 각자의 `feature/*` 브랜치에서 한다
