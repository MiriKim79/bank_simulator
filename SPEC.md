# SPEC — 「금융한걸음」

> 고령자를 위한 AI 모바일 뱅킹 학습 서비스

| 항목 | 값 |
|---|---|
| 작성일 | 2026-08-18 (2차 개정 2026-08-19 · 3차 수정 2026-08-19) |
| 팀 규모 | 3명 |
| 기간 | 1일 |
| 목표 산출물 | MVP (동작하는 웹 데모) |
| 문서 상태 | MoSCoW 2차 확정(10절) 전체 반영 완료. 3차 수정으로 AI 인터페이스·미션 개수·진행 표시 확정. **UI/UX 레퍼런스 대조는 13절, 팀 결정 대기 5건은 12절** |

---

## 1. 목표 (무엇을 만드나)

실제 돈을 쓰지 않는 **가상의 모바일 뱅킹 환경**에서, 고령자가 송금을 **스스로 처음부터 끝까지** 해보며 익히는 웹 학습 서비스.

핵심 방향은 "AI가 대신 해주는 서비스"가 아니라 **"스스로 할 수 있게 가르쳐주는 서비스"**다. 따라서 AI는 사용자를 대신해 버튼을 누르지 않고, 설명·안내·피드백만 한다.

---

## 2. 사용자 (누가 쓰나)

- **주 사용자**: 스마트폰은 쓰지만 모바일 뱅킹은 처음이거나 서투른 고령자.
  - 예시 상황: "아들이 계좌번호를 문자로 보내줬는데, 은행 앱에서 어떻게 돈을 보내는지 몰라서 결국 은행 창구에 간다."
- **전제**: 사용자는 화면을 손으로 누를 수 있고, 소리를 들을 수 있으며, 마이크에 대고 말할 수 있다.
- `[확인 필요]` 실제 고령자를 대상으로 사용성 테스트를 할 것인가, 아니면 팀 외 사람(동료·심사자)에게 시연하는 것으로 검증을 갈음할 것인가.
- `[확인 필요]` 보호자(자녀)가 옆에서 같이 쓰는 시나리오를 고려할 것인가. (현재 SPEC은 사용자 혼자 쓰는 것을 전제)

---

## 3. 문제 (왜 필요한가)

1. 은행 업무가 모바일 중심으로 이동하는 중인데, 고령자는 진입에서 막힌다.
2. **연습할 방법이 없다.** 실제 뱅킹 앱에서 이것저것 눌러보며 배우려 해도, 송금은 잘못 누르면 실제 돈이 움직인다. 그래서 "일단 해보면서 익히기"라는 가장 자연스러운 학습 방식이 봉쇄돼 있다.
3. 옆에서 알려줄 사람이 항상 있지도 않다. 자녀가 한 번 알려줘도, 혼자 다시 하려 하면 기억이 나지 않는다.

→ **실수해도 안전한 환경에서, 옆에서 알려주는 사람 역할을 AI가 대신하며, 몇 번이고 반복 연습할 수 있게 한다.**

---

## 4. 범위

### 4.1 포함 (In Scope)

10절 MoSCoW 2차 확정 기준. Won't 3건은 4.2로 이동했다.

| # | 기능 | 상세 | 등급 |
|---|---|---|---|
| F3 | 가상 홈 화면 | 가짜 계좌번호·잔액과 이체/거래내역/조회 버튼이 있는 첫 화면 | Must |
| **F4+F15** | **송금 6단계 화면 (완료 화면 포함)** | 이체 → 은행 선택 → 계좌번호 → 금액 → 확인 → 완료. 완료 화면에서 성공을 알리고 다시 연습하기로 돌아감 | Must |
| F17 | 진행 단계 표시 | 화면 안에 작게 `2 / 5 단계 — 계좌번호 입력`. **F4+F15 화면 안의 구성요소** | Must |
| F6 | 미션 데이터 스키마 | 단계 목록·정답 액션·오답 문구·단계 이름을 담는 JSON 구조 + **송금 미션 1개** (4.3 확정) | Must |
| F7 | 미션 상태머신 | 현재 단계를 들고 있다가 입력을 받아 정답이면 다음 단계로 넘김 | Must |
| F8 | 오답 피드백 | 틀린 버튼을 누르면 화면은 그대로 두고 흔들림 효과 + 왜 틀렸는지 설명 | Must |
| F13 | askAI 폴백 인터페이스 | `askAI(question, currentStep, mission)` — LLM 먼저 시도, 실패하면 미리 쓴 답변에서 매칭해 반환. **미션 데이터를 인자로 받는다** | Must |
| F14 | 도움 진입점 | "AI에게 물어보기"·"다시 설명해주세요"를 어느 화면에서나 눌리는 고정 위치에 배치 | Must |
| F1 | 폰 셸 | 세로형 폰 프레임 + 큰 글씨·큰 버튼 기본 스타일 | Should |
| F5 | 큰 숫자 키패드 | 계좌번호·금액 입력용 대형 숫자 키패드 | Should |
| F12 | 마이크 질문 (STT) | 마이크 버튼을 눌러 말하면 브라우저가 텍스트로 변환 | Should |
| F16 | 금융용어 미니 퀴즈 | 송금에서 실제 나온 용어를 객관식 3~5문제로 복습. 정답·오답 즉시 쉬운 설명. 점수·기록 저장 없음 | Should |
| F2 | 용어 병기 라벨 | 실제 용어("이체") + 아래 작은 글씨로 쉬운 말("돈 보내기") | Could |

**F4와 F15는 한 항목으로 묶었다(10절 팀 확정).** 완료 화면은 송금 흐름의 마지막 단계이므로 6단계 화면 세트에 포함해 만들고 함께 판정한다.

**AI 답변 출력은 화면 텍스트다.** 음성 낭독(F11)이 Won't이므로 askAI 결과는 읽어주지 않고 화면에 띄운다.

**질문 입력 수단은 텍스트가 기본이다.** 마이크(F12)가 Should로 내려갔으므로, Must만 만든 상태에서 사용자가 질문하는 경로는 F14의 텍스트 입력칸 하나다.

**askAI는 미션 데이터를 받는다.** F6이 Must이므로 단계·정답·단계 이름은 `missions.json` 에 있다. askAI가 질문과 단계 이름만 받으면 "지금 무엇을 해야 하나요"에 답할 근거가 코드 안에 다시 하드코딩되고, F6과 AI가 반쪽만 연결된다. 시그니처는 8절에 적었다.

#### F17 진행 단계 표시 — F10과 무엇이 다른가

F10(Won't)과 헷갈리기 쉬우므로 구분을 못 박는다. **둘은 다른 항목이다.**

| | F17 진행 단계 표시 (Must) | F10 단계별 안내 배너 (Won't) |
|---|---|---|
| 무엇을 말하나 | **지금 어디쯤인가** — 위치 | **지금 무엇을 하라** — 지시 |
| 문구 예 | `2 / 5 단계 — 계좌번호 입력` | `받는 분 계좌번호를 숫자로 눌러 주세요` |
| 크기·위치 | 화면 안 작은 글씨 1줄 | 화면 상단 항상, 큰 글씨 |
| 문구 출처 | `missions.json` 의 `label` (고정) | 단계마다 따로 쓴 안내문 |
| 만드는 비용 | A의 화면 파일 한 줄 + `showStep` 안에서 갱신 | 단계별 문안 작성 + 배너 레이아웃 |

F17을 Must에 넣는 이유: F10·F11이 둘 다 Won't라 **10.2 리스크 2**(다음 행동 안내 경로가 없다)가 남아 있는데, F17은 그 리스크를 가장 싸게 줄인다. 사용자가 "몇 개 더 남았나"를 알면 중간에 포기하지 않는다.

**MoSCoW Must 항목 수는 그대로 7개다.** F17은 새 기능 항목이 아니라 **이미 Must인 F4+F15 화면 안의 구성요소**로 넣었다 (F4+F15 안에 기본 `<input>` 이 포함되는 것과 같은 방식). 10절 확정본을 건드리지 않는다.

| F17 조각 | 어디에 | 누가 |
|---|---|---|
| `label` 필드 (`"계좌번호 입력"`) | `missions.json` | S0에서 셋이 같이, 이후 B |
| `#progress` 요소 1줄 | `bank-ui.html` 각 화면 | A |
| `showStep` 이 `label` 을 읽어 갱신 | `bank-ui.html` 인라인 `BankUI` | A |

분모 5의 근거: 진행 표시는 이체를 누른 뒤부터 센다. `bank`=1 · `account`=2 · `amount`=3 · `confirm`=4 · `done`=5. `home` 은 미션 시작 전이므로 표시하지 않는다(`label` 빈 문자열).

#### F16 금융용어 미니 퀴즈 — 상세 (Should)

송금 실습에서 헷갈릴 수 있는 용어를 복습하는 짧은 객관식 퀴즈.

| 항목 | 값 |
|---|---|
| 문제 수 | 3~5개 (MVP는 5개) |
| 형식 | 객관식 4지선다 |
| 용어 출처 | **송금 6단계에서 실제 화면에 등장한 용어만** |
| 피드백 | 정답·오답을 고른 즉시, 쉬운 말 설명 |
| 저장 | 점수·학습 기록 저장 없음 |
| 진입 | 완료 화면(F15)의 "용어 복습하기" 버튼 |
| 종료 | 마지막 문제 후 완료 화면으로 돌아감 |

**용어 목록**

| 용어 | 쉬운 말 |
|---|---|
| 이체 | 돈 보내기 |
| 출금계좌 | 돈이 빠져나가는 내 통장 |
| 계좌번호 | 돈을 받을 통장을 구분하는 번호 |
| 잔액 | 통장에 남아 있는 돈 |
| 수취인 / 받는 사람 | 돈을 받는 사람 |

**문제 예시**

> 돈을 보낼 때 돈이 빠져나가는 내 통장을 무엇이라고 할까요?
> ① 출금계좌 ② 계좌번호 ③ 잔액 ④ 거래내역

**흐름**: 퀴즈 시작 → 문제 3~5개 → 문제마다 즉시 정답/오답 + 설명 → 완료 → 완료 화면 복귀

**오답 처리 = 설명 후 다음 문제.** 다시 고를 기회는 주지 않는다. 오답을 고르면 그 선택지를 틀렸다고 표시하고, 정답과 쉬운 설명을 함께 보여준 뒤 "다음 문제" 버튼만 남긴다. F9(반복 오답 힌트 상승)가 Won't인 것과 같은 방향이다.

> 이 값은 **팀 결정 대기 항목이다 — 12절 2번.** 위 문장이 SPEC의 현재 확정값이고, 코드(`quiz.js`)도 이대로 만든다. 팀이 "다시 선택"으로 뒤집으면 바뀌는 곳은 `quiz.js` 의 `answer()` 하나다.

### 4.2 범위 밖 (Out of Scope)

- 실제 은행 계좌 연동, 실제 송금, 오픈뱅킹 API
- 로그인·회원가입·개인정보 저장
- 네이티브 앱 (iOS/Android 앱스토어 배포)
- 다국어
- 학습 이력의 서버 저장·계정별 관리
- 접근성 인증 수준의 스크린리더 대응
- **실제 은행의 브랜드 복제** — 로고·브랜드명·고유 색상·이미지·아이콘을 그대로 쓰지 않는다. 화면은 **가상 은행**으로 만든다 (13.1)

**MoSCoW Won't (10절 팀 확정 · 1차 2026-08-18, 2차에서도 유지)**

| # | 기능 | 한 줄 설명 |
|---|---|---|
| F9 | 반복 오답 힌트 단계 상승 | 같은 단계에서 반복해 틀린 횟수를 세어 설명 문구를 점점 더 쉬운 것으로 바꾼다 |
| F10 | 단계별 안내 배너 | 지금 무엇을 해야 하는지 화면 상단에 항상 큰 글씨로 띄운다 |
| F11 | 음성 낭독 (TTS) | 안내·오답 설명·AI 답변을 브라우저 내장 음성으로 읽어준다. **등급 재검토 대기 — 12절 1번** |

### 4.3 미션 범위 — 확정 (2026-08-19 3차)

**미션은 송금 1개다.** 이전에 4.1의 F6에서는 "송금 미션 1개"라고 정의하면서 여기서는 개수를 미결로 남겨 문서가 자기 모순이었다. 아래로 닫는다.

| 미션 | 결정 |
|---|---|
| 송금 (김민수에게 5만원) | **포함. 이번 MVP의 유일한 미션** |
| 금융사기 예방 (검찰 사칭 → 안전계좌 유도) | **이번 MVP 제외** |
| 잔액 조회 | **이번 MVP 제외** |
| 거래내역 보기 | **이번 MVP 제외** |
| 학습 결과·틀린 부분 분석 화면 | **이번 MVP 제외** |

미션이 1개이므로 **미션 목록·선택 화면을 만들지 않는다.** 앱을 열면 바로 송금 미션이 시작된다.

`missions.json` 은 **미션 1개를 담는 객체 하나**다. 배열로 감싸지 않는다. 나중에 여러 미션이 필요해지면 그때 배열로 바꾸고 목록 화면을 붙인다 — MVP에서는 그 구조를 미리 만들지 않는다.

금융사기 미션을 뺀 이유를 남겨 둔다: 이 미션은 **"송금하지 않는 것"이 정답**이라 성공 조건이 송금 미션과 반대다. 미션 엔진에 "이 단계의 정답은 중단하는 것"이라는 개념이 하나 더 필요하고, F7이 Must인 상태에서 1일 안에 두 종류의 판정 규칙을 만들면 Must가 위험해진다. 발표 차별점으로는 아깝지만 다음 순번이다.

---

## 5. 제약

### 5.1 확정된 제약

| 구분 | 내용 |
|---|---|
| 인원 | 3명 |
| 기간 | 1일 |
| 산출물 | MVP만 — 완성도 있는 데모 1개 |
| 플랫폼 | **웹** (네이티브 앱 아님) |
| 데이터 | 전부 가짜. 실제 금융 데이터·실제 개인정보 미사용 |

### 5.2 기술 제약 (조사로 확인된 사실)

- **iOS에서는 음성 질문(STT)이 동작하지 않는다.** Web Speech API의 음성 인식은 iOS Safari에서 미지원이고, iOS의 Chrome도 내부 엔진이 Safari라 동일하다.
  - F12가 Should로 내려가면서 이 제약의 위험도가 낮아졌다. MVP에는 음성이 없으므로 **아이폰으로 시연해도 Must 기능은 하나도 죽지 않는다.** F12를 구현한 뒤에만 문제가 된다.
  - 음성 안내(TTS)는 iOS에서도 동작한다.
  - 음성 인식이 확실히 동작하는 조합은 **데스크톱 Chrome / Android Chrome**이다.
- **마이크 권한**은 보안 컨텍스트를 요구할 수 있다. `screen/bank-ui.html` 을 `file://` 로 더블클릭해 여는 방식에서는 마이크가 차단될 수 있으므로 `localhost` 또는 https로 띄워야 할 가능성이 높다.
- **API 키를 프론트엔드에 넣으면 페이지 소스에 그대로 노출된다.** 정적 웹만으로는 키를 숨길 수 없다.

### 5.3 `[확인 필요]` 제약

- `[확인 필요]` **LLM 호출 경로**: Anthropic·OpenAI 키 직접 호출 / 외부 LLM 미사용 중 무엇인가. (AWS는 팀 결정으로 제외)
- `[확인 필요]` **API 키 보유 여부와 비용 부담 주체**.
- `[확인 필요]` **시연 환경**: 데스크톱 Chrome인가, Android 실기기인가. (5.2의 STT 제약과 직결)
  - 현재 개발 가정 = 데스크톱 Chrome. 화면 안에 세로형 폰 프레임을 그려 모바일처럼 보이게 함. 폰 브라우저에서 레이아웃이 깨지지 않게는 하되 폰 검증은 하지 않음. **이 가정은 팀 승인 전이다.**
- `[확인 필요]` 배포 방식: 로컬 실행만인가, 공개 URL이 필요한가. (코드를 공개 저장소에 올릴 경우 API 키 직접 삽입 방식은 불가)

---

## 6. 성공 기준

### 6.1 Must만으로 판정하는 기준 (이걸 못 넘기면 실패)

1. **완주**: 팀원이 아닌 사람이 별도 설명 없이 송금 미션을 끝까지 완료하고 완료 화면까지 도달한다. (F4+F15)
   - 각 화면에 `n / 5 단계 — 단계 이름` 이 보인다 (F17). 문구는 `missions.json` 의 `label` 에서 온다.
2. **텍스트 질문 왕복**: 도움 진입점에서 질문을 적어 보내면, 그 답변이 **화면 텍스트로** 표시된다. (F14 + F13)
   - 마이크 질문은 이 기준에서 빠졌다. F12가 Should로 내려갔기 때문이다. 6.2 기준 7로 옮겼다.
3. **폴백 성립**: 외부 LLM이 연결되지 않은 상태에서도 위 1~2가 전부 동작한다. 발표 중 네트워크·API 사고로 데모가 죽지 않는다.
4. **오답 회복** (F8): 일부러 틀린 버튼을 눌렀을 때, 화면이 넘어가지 않고 왜 틀렸는지와 무엇을 눌러야 하는지를 안내받아 스스로 다시 진행할 수 있다.
5. **막힘 없음**: 어떤 단계에서도 사용자가 다음에 뭘 해야 할지 알 수 없어 멈추지 않는다.
   - 화면 안내 배너(F10)와 음성 안내(F11)는 여전히 둘 다 Won't다. 그러나 F8이 Must로 올라와서 달성 경로가 둘이 됐다. **틀리게 눌러 F8의 설명을 받는 경로**와 **스스로 F14를 눌러 묻는 경로**다. 이전 분류에서 F14 하나에 몰려 있던 위험이 줄었다.
   - F17(진행 표시)이 세 번째 경로를 보탠다. 다만 F17은 **위치만 알려주고 지시는 하지 않는다.** "지금 무엇을 하라"는 여전히 F8·F14로만 온다. F17을 F10의 대체물로 세지 않는다.
6. **미션이 데이터로 분리됨** (F6): 단계·정답·오답 문구·단계 이름(`label`)이 `missions.json` 에 있고, 그 파일만 고쳐서 미션 내용을 바꿀 수 있다. 상태머신 코드를 고치지 않는다.
   - 판정 근거는 이 파일 하나다. **AI 답변 경로도 이 파일을 본다** — `askAI` 가 세 번째 인자로 미션 객체를 받으므로, 미션이 바뀌면 AI가 참조하는 내용도 같이 바뀐다. 이게 안 되면 F6과 AI의 연결이 반쪽이다.

### 6.2 Should 구현 시 추가로 판정하는 기준

7. **음성 질문 왕복** (F12): 마이크 버튼을 눌러 말로 질문하면, 그 답변이 화면 텍스트로 표시된다. 인식에 실패하면 텍스트 입력으로 되돌아간다.
8. **고령자 가독성** (F1): 팔 길이 거리에서 버튼 글씨를 읽을 수 있고, 버튼을 정확히 겨냥하지 않아도 눌린다.
9. **키패드 입력** (F5): 물리 키보드를 쓰지 않고 화면의 대형 숫자 키패드만으로 계좌번호와 금액을 넣을 수 있다.
10. **용어 퀴즈 완주** (F16): 완료 화면에서 퀴즈에 들어가 3~5문제를 끝까지 풀 수 있고, 매 문제마다 정답 여부와 쉬운 설명을 즉시 받는다. 끝나면 완료 화면으로 돌아온다. 점수는 저장하지 않는다.
    - 퀴즈에 나오는 용어는 **송금 6단계에서 실제로 화면에 등장한 것만** 쓴다. 안 본 용어를 묻는 것은 학습이 아니라 시험이다.

### 6.3 `[확인 필요]` 기준

- `[확인 필요]` 사용자 테스트를 몇 명에게, 어떤 방식으로 할 것인가. (예: "60대 이상 1명에게 시켜보고 완주하면 통과")
- `[확인 필요]` 발표 심사 기준이 따로 공지되었는가. 있다면 그 기준을 성공 기준에 반영해야 한다.
- `[확인 필요]` 음성 인식 성공률의 목표치. (발표장 소음·고령자 발화 특성상 100%는 불가능하므로, "인식 실패 시 화면 버튼으로 폴백되면 통과" 같은 기준이 필요)

---

## 7. MVP 정의

> **한 문장**: 데스크톱 Chrome에서 열면 가상 은행 화면에서 "김민수에게 5만원 보내기"를 완료 화면까지 수행할 수 있고, 화면마다 몇 번째 단계인지 보이고, 틀리게 누르면 왜 틀렸는지 설명을 받고, 막히면 질문을 적어 보내 화면에 뜬 답을 읽고 다음으로 갈 수 있다. 미션은 송금 1개이고, 그 내용은 `missions.json` 만 고쳐서 바꿀 수 있다.

### MVP = Must 7개

| # | 기능 |
|---|---|
| F3 | 가상 홈 화면 |
| F4+F15 | 송금 6단계 화면 (완료 화면 포함) — **F17 진행 표시 포함** |
| F6 | 미션 데이터 스키마 (`missions.json`) — 미션 1개 |
| F7 | 미션 상태머신 |
| F8 | 오답 피드백 |
| F13 | askAI 폴백 인터페이스 (LLM 실패 시 미리 쓴 답변) |
| F14 | 도움 진입점 |

### MVP에서 빠지는 것

- 단계별 안내 배너 (F10), 음성 낭독 (F11), 반복 오답 힌트 강화 (F9) — Won't
- 폰 셸·큰 글씨 스타일 (F1), 숫자 키패드 (F5), 마이크 질문 (F12), 금융용어 미니 퀴즈 (F16) — Should. 착수 순서는 11.5
- 용어 병기 (F2) — Could
- 로그인·계정·저장, 실제 계좌 연동, 미션 목록 화면, 학습 결과 분석
- 금융사기 예방 미션, 잔액 조회, 거래내역 — **4.3에서 이번 MVP 제외로 확정**

### F5가 Should라서 생기는 공백

F4+F15에는 계좌번호·금액 **입력** 단계가 있는데 F5(키패드)가 Should다. Must만 만들면 입력 수단이 없다. 최소한 기본 `<input>` 하나는 F4+F15 안에 포함해야 그 항목이 성립한다. F5는 그걸 고령자용 대형 키패드로 바꾸는 작업으로 본다.

### F12가 Should라서 생기는 공백

F14(도움 진입점)의 원래 그림은 "마이크를 눌러 말로 묻는다"였다. F12가 Should로 내려갔으므로 **F14 안에 텍스트 입력칸과 보내기 버튼이 Must로 포함돼야** 질문 경로가 성립한다. F12는 나중에 그 입력칸 옆에 마이크 버튼을 더하는 작업으로 본다.

결과적으로 **MVP에는 음성이 전혀 없다.** F11(낭독) Won't + F12(인식) Should. 팀이 처음 적은 필수기능 목록의 "음성 질문 + 음성 안내"가 MVP 밖으로 나갔다는 뜻이다. 10.2 리스크 4에 기록해 뒀다.

---

## 8. 설계 결정 (인터뷰 결과 + MoSCoW 반영)

| # | 결정 | 인터뷰 시점 | 10절 MoSCoW 반영 후 |
|---|---|---|---|
| D1 | 음성 범위 | STT + TTS 둘 다 Must | **둘 다 MVP 밖. STT(F12)는 Should, TTS(F11)는 Won't** |
| D2 | 답변 생성 주체 | LLM 우선 → 실패 시 미리 쓴 답변 폴백 | 유지 (F13 Must) |
| D3 | 음성 구현 수단 | 브라우저 내장 Web Speech API | 유지 (인식만 사용) |
| D4 | LLM 호출 경로 | `[확인 필요]` — 폴백 기준으로 개발 | 유지 |
| D5 | 플랫폼 | 웹 | 유지 |
| D6 | 금융용어 처리 | 실제 용어 + 아래 쉬운 말 병기 | 방식 유지, 등급은 **Could(F2)** |
| D7 | 오답 시 화면 | 화면 고정 + 흔들림 + 설명만 | 방식 유지, 등급은 **Must(F8)** |
| D8 | 힌트 강화 방식 | 문구만 쉬워짐 | **무효 — F9 Won't** |
| D9 | 미션 데이터 위치 | (인터뷰 때 없던 결정) | **`missions.json` 파일로 분리 — F6 Must** |
| D10 | 질문 입력 수단 | 마이크 | **텍스트 입력이 기본. 마이크는 Should(F12)로 추가** |
| D11 | 미션 개수 | 미결(Q1) | **송금 1개로 확정. 목록 화면 없음, `missions.json` 은 객체 하나 — 4.3** |
| D12 | 진행 안내 | 큰 안내 배너(F10) | **F10은 Won't 유지. 대신 작은 진행 표시(F17)를 F4+F15 안에 넣는다 — 4.1** |

### 설계 결정에서 파생되는 인터페이스 요구

D2(폴백 구조) 때문에, AI 답변을 요청하는 지점은 **함수 하나로 감싸야 한다.** 그래야 D4가 나중에 정해져도 그 함수 내부만 바꾸면 된다.

```
askAI(question, currentStep, mission) → Promise<답변텍스트>
  ├─ 외부 LLM 경로가 설정돼 있으면 그쪽으로 (타임아웃 필요)
  ├─ 실패하거나 미설정이면 미리 쓴 답변에서 매칭
  └─ 그것도 없으면 "화면의 버튼을 눌러보세요" 같은 기본 답변
```

| 인자 | 무엇 | 왜 필요한가 |
|---|---|---|
| `question` | 사용자가 적은(또는 말한) 질문 문자열 | 질문 내용 |
| `currentStep` | 단계 이름 6개 중 하나 (`'account'` 등) | 지금 어느 화면인지 |
| `mission` | **`missions.json` 에서 읽은 미션 객체 통째로** | 이 단계의 정답 `action`·기대 `value`·`label`, 다음 단계, 오답 문구를 AI가 근거로 쓴다 |

**세 번째 인자가 없으면 F6이 반쪽이 된다.** `mission` 없이 `currentStep` 만 넘기면, "지금 무엇을 해야 하나요"에 답하려고 단계별 설명을 `ai-guide.js` 안에 다시 적게 된다. 그러면 미션 내용이 `missions.json` 과 `ai-guide.js` 두 곳에 생기고, `missions.json` 만 고쳐도 미션이 바뀐다는 F6의 약속이 깨진다. AI 답변은 판정과 **같은 데이터**를 봐야 한다.

`mission` 은 C가 만들지 않는다. **B가 `MissionEngine.getMission()` 으로 내준다.** 로드 전이면 `null` 이고, `askAI` 는 `null` 이어도 반드시 답변을 돌려준다.

반환된 답변은 **화면에 텍스트로 표시한다.** 낭독하지 않는다(F11 Won't · 등급 재검토는 12절 1번).

D9(F6 Must)로 **공유 데이터 구조가 돌아왔다.** 이전 분류에서 F6이 Could였을 때는 단계·정답·문구가 B의 코드 안에만 있어서 11절의 3인 분할이 구두 조율에 의존했다. 이제 `missions.json` 이 A·B·C의 공통 참조점이 된다.

```
missions.json  ← 단계 6개 / 정답 action·value / 오답 문구 / label(F17 진행 표시)
   ├─ B: 이 파일을 읽어 판정한다 (mission-engine.js)
   │      + getMission() 으로 이 객체를 그대로 내준다
   ├─ A: 이 파일의 action 값에 맞춰 data-action 을 붙인다 (bank-ui.html)
   │      + label 을 읽어 진행 표시를 갱신한다 (F17)
   └─ C: 이 파일의 단계 이름에 맞춰 답변을 쓴다 (qa-data.json)
          + askAI 의 3번째 인자로 이 객체를 받아 답변 근거로 쓴다 (F13)
```

**세 레인이 모두 이 파일 하나를 본다.** A는 `label`, B는 판정, C는 `askAI` 의 `mission` 인자로. 미션을 바꾸려면 이 파일만 고치면 된다는 F6의 약속이 여기서 성립한다.

세 사람이 같은 파일을 동시에 고치면 안 되므로, `missions.json` 은 **11.1에서 셋이 같이 한 번 확정한 뒤 B만 수정한다.**

---

## 9. `[확인 필요]` 전체 목록

이 SPEC에서 아직 정해지지 않아 추측하지 않고 남긴 항목이다.

### 9.1 닫힌 항목 (2026-08-19 3차)

| # | 항목 | 결정 | 어디에 |
|---|---|---|---|
| Q1 | 미션 개수 | **송금 1개.** 미션 목록 화면 없음 | 4.3, D11 |
| Q2 | 금융사기 예방 미션 | **이번 MVP 제외.** 중단이 정답인 판정 규칙을 1일 안에 F7과 같이 만들면 Must가 위험하다 | 4.3 |

Q1은 4.1의 F6에서 이미 "송금 미션 1개"로 정의해 놓고 9절에서 다시 미결로 남겨 둔 상태였다. 문서 안에서 미션 개수가 서로 달랐던 것이고, 위로 닫았다.

### 9.2 남은 항목

| # | 항목 | 이걸 정해야 결정되는 것 |
|---|---|---|
| Q3 | LLM 호출 경로 (직접 키 / 미사용) | 키 노출 대응, 배포 방식 |
| Q4 | API 키 보유 여부·비용 부담 주체 | Q3의 전제 |
| Q6 | 시연 환경 (데스크톱 Chrome / Android 실기기) | STT 동작 여부, UI 크기 기준 |
| Q7 | 배포 방식 (로컬 / 공개 URL) | 키 직접 삽입 가능 여부, 마이크 권한 |
| Q8 | 사용성 테스트 대상·방법 | 성공 기준 6.2 |
| Q9 | 발표 심사 기준 공지 여부 | 성공 기준 6.1 우선순위 |
| Q10 | 음성 인식 실패 시 UX (버튼 폴백 형태) | STT 담당 작업 범위 |
| Q11 | 보호자 동반 시나리오 고려 여부 | 사용자 정의 |

---

## 10. MoSCoW (팀 2차 확정 · 2026-08-19)

9절 기능 후보 15개에 대한 팀 결정. 아래가 확정본이며, 4.1 범위표와 7절 MVP 정의보다 우선한다.

> 등급 안의 나열 순서는 의미 없다(팀 확인). 문서 전체에서 번호순으로 정렬해 둔다. 착수 순서는 11.5에 따로 적는다.

**F4와 F15를 한 항목으로 묶었다.** 완료 화면은 송금 6단계의 마지막이므로 따로 세지 않는다. Must는 7항목이다.

**3차 수정(2026-08-19)은 이 표의 등급을 바꾸지 않았다.** F17(진행 표시)은 F4+F15 안의 구성요소로 넣었고, F11의 등급 재검토와 F16의 오답 처리는 12절에 결정 대기로 남겼다. Must는 그대로 7항목이다.

### Must — 없으면 프로젝트 실패
| # | 기능 |
|---|---|
| F3 | 가상 홈 화면 |
| F4+F15 | 송금 6단계 화면 (완료 화면 포함, 기본 `input`·**F17 진행 표시** 포함) |
| F6 | 미션 데이터 스키마 (송금 미션 1개) |
| F7 | 미션 상태머신 |
| F8 | 오답 피드백 |
| F13 | askAI 폴백 인터페이스 |
| F14 | 도움 진입점 |

### Should — 가치 있지만 미룰 수 있음
| # | 기능 |
|---|---|
| F1 | 폰 셸 (큰 글씨·큰 버튼 기본 스타일 포함) |
| F5 | 큰 숫자 키패드 |
| F12 | 마이크 질문 (STT) |
| F16 | 금융용어 미니 퀴즈 |

### Could — 있으면 좋은 장식
| # | 기능 |
|---|---|
| F2 | 용어 병기 라벨 |

### Won't — 이번엔 안 함
| # | 기능 |
|---|---|
| F9 | 반복 오답 힌트 단계 상승 |
| F10 | 단계별 안내 배너 |
| F11 | 음성 낭독 (TTS) |

### 10.0 1차(2026-08-18) 대비 바뀐 것

| # | 기능 | 1차 | 2차 | 이 변경이 만드는 결과 |
|---|---|---|---|---|
| F4·F15 | 송금 화면 / 완료 화면 | 각각 Must | **한 항목** | 항목 수만 줄었다. 만들 것은 같다 |
| F6 | 미션 데이터 스키마 | Could | **Must** | 단계·정답·문구가 `missions.json` 으로 나온다. 1차 리스크 1 해소, 3인 병렬 분할이 스키마 기반으로 복귀 |
| F8 | 오답 피드백 | Should | **Must** | 1차 리스크 3 해소. 6.1에 오답 회복 기준이 들어오고, 막힘 없음 기준의 달성 경로가 둘로 늘어 리스크 2도 완화 |
| F12 | 마이크 질문 (STT) | Must | **Should** | **MVP에서 음성이 전부 빠진다.** iOS 시연 위험은 사라지지만 발표 차별점이 약해진다. 리스크 4로 기록 |
| F17 | 진행 단계 표시 | (F10 배너에 묶여 Won't 취급) | **Must — F4+F15 안** | F10과 다른 항목임을 4.1에서 못 박았다. 위치만 알려주는 작은 1줄이라 비용이 거의 없고, 리스크 2를 싸게 줄인다 |
| F16 | 금융용어 미니 퀴즈 | (없던 기능) | **Should** | 신규. 완료 화면 뒤에 붙는 독립 모듈이라 Must 경로를 건드리지 않는다. F12가 빠져 약해진 차별점을 일부 메울 수 있다 |

### 10.1 문서 전체 반영 완료

2차 MoSCoW를 SPEC 전체에 적용했다. 바뀐 위치:

| 절 | 반영 내용 |
|---|---|
| 4.1 | F4+F15 한 행으로 병합. F6·F8을 Must로, F12를 Should로 이동. F16 신규 추가 + 상세 소절 |
| 5.2 | iOS STT 제약의 위험도 하향 (MVP에 음성이 없으므로 Must 기능이 죽지 않음) |
| 6.1 | 기준 2를 "텍스트 질문 왕복"으로 교체. 오답 회복(F8)과 미션 데이터 분리(F6)를 Must 기준으로 승격. 막힘 없음의 달성 경로가 둘로 늘어난 것 반영 |
| 6.2 | 음성 질문 왕복(F12)을 조건부로 이동. 키패드(F5)·용어 퀴즈(F16) 기준 신규 |
| 7 | MVP = F3·F4+F15·F6·F7·F8·F13·F14. MVP 한 문장에서 마이크 삭제. "F12가 Should라서 생기는 공백" 소절 신규 |
| 8 | D1 수정(둘 다 MVP 밖), D7 등급 Must, D9·D10 신규. F6 Must로 공유 데이터 구조 복귀 |
| 11 | 스키마 기반 3인 병렬 분할로 재작성. 파일 소유권·스텁·동기화 지점 추가 |

### 10.1.1 3차 수정(2026-08-19)에서 바뀐 곳

등급은 건드리지 않았다. 문서 안의 모순과 빠진 것을 메웠다.

| 절 | 무엇 |
|---|---|
| 4.1 | F13 시그니처에 `mission` 인자 명시. **F17 진행 표시 신규 + F10과의 차이 표**. F16 오답 처리를 한 방향으로 확정 |
| 4.2 | F11 행에 등급 재검토 대기 표시 (12절) |
| 4.3 | 미결 목록 → **미션 범위 확정표**. 송금 1개, 나머지 미션 MVP 제외 |
| 6.1 | 기준 1에 진행 표시, 기준 5에 F17의 한계(위치만 알려준다), 기준 6에 AI도 같은 데이터를 본다 |
| 7 | MVP 한 문장에 진행 표시·미션 1개 반영 |
| 8 | **`askAI(question, currentStep, mission)` 로 시그니처 교체** + 인자표 + 왜 3번째가 필요한가. `MissionEngine.getMission()` 신설. D11·D12 추가 |
| 9 | Q1·Q2를 9.1 닫힌 항목으로 이동 |
| 11.1 | 선행 합의에 `label` 과 인자 3개를 못 박을 항목으로 추가 |
| **12 (신규)** | 팀 결정 대기 2건 — F11 등급, F16 오답 처리 |

### 10.2 리스크 재평가

1차(2026-08-18)에 기록한 리스크 3건이 2차 분류에서 어떻게 됐는지, 그리고 새로 생긴 것.

| # | 리스크 | 상태 |
|---|---|---|
| 1 | F6이 Could인데 F7이 Must — 스키마 없이 상태머신을 만들면 단계·정답·문구가 코드에 하드코딩되고 3인 병렬 분할이 성립하지 않는다 | **해소.** F6이 Must가 되어 `missions.json` 이 A·B·C 공통 참조점으로 돌아왔다 |
| 2 | F10·F11 동시 Won't — 다음 행동을 알려주는 경로가 화면·소리 양쪽에 없어 F14 하나에 몰린다 | **완화(3차에서 추가 완화).** F8이 Must가 되어 "틀리게 눌러 설명받는" 경로가 생겼고, F17(진행 표시)이 "어디쯤인가"를 항상 보여준다. 다만 **지시 문구는 여전히 없다** — F17은 위치만 말한다. F11을 Should로 올릴지가 12절 1번 |
| 3 | F8이 Should — 오답 설명이 차별점의 실체인데 미루면 가짜 은행 앱과 구분이 어렵다 | **해소.** F8이 Must가 됐다 |
| 4 | **F12가 Should + F11이 Won't — MVP에 음성이 하나도 없다** | **신규.** 팀이 처음 적은 필수기능 목록의 "음성 질문 + 음성 안내"가 MVP 밖으로 나갔다. iOS 시연 위험은 사라졌지만, 발표에서 "AI를 어디에 썼나"라는 질문에 답할 근거가 F13(askAI) 하나로 줄어든다. F16이 일부 메우지만 F16도 Should다. **Must만 만든 상태로 발표하게 되면 이 항목을 어떻게 설명할지 미리 정할 것** |

---

## 11. 3명 병렬 작업 분할 (2차 MoSCoW 반영)

F6이 Must로 올라왔으므로 `missions.json` 을 다시 인터페이스로 삼는다. 여기에 **파일 소유권**과 **스텁**을 더해, 세 사람이 서로를 기다리지 않고 각자 끝까지 갈 수 있게 만든다.

### 11.1 선행 — 셋이 같이 (30분 이내)

시작 전에 넷을 확정한다. 안 하면 통합이 불가능하다.

1. **단계 이름 6개 고정** — `home`, `bank`, `account`, `amount`, `confirm`, `done`
   - 퀴즈 화면(F16)은 미션 단계가 아니지만 화면 전환은 같은 함수를 쓴다. 화면 이름은 7개, 그중 미션 단계는 6개다.
2. **`missions.json` 확정** — 단계 6개의 `action` / `value` / `wrong` 문구 / **`label`(F17 진행 표시 문구)**. **이 파일은 여기서 한 번 같이 쓰고, 이후에는 B만 고친다.**
3. **DOM 규칙** — 버튼 `data-action` 값, 입력칸·도움바 id, 화면 `id` 패턴
4. **전역 함수 시그니처** — `BankUI` / `MissionEngine` / `AIGuide` / `Voice` (+ Should 단계에서 `Quiz`)
   - 여기서 반드시 못 박을 두 개: `AIGuide.askAI(question, currentStep, mission)` 의 **인자 3개**와, 그 `mission` 을 내주는 `MissionEngine.getMission()`. 인자를 2개로 만들면 C가 미션 설명을 `ai-guide.js` 에 다시 적게 되고 F6이 반쪽이 된다(8절).

결과는 `shared/CONTRACT.md` 에 적는다. 고칠 때는 셋이 같이 고친다.

### 11.2 파일 소유권 — 한 폴더에 한 사람

병렬 작업이 깨지는 가장 흔한 원인은 두 사람이 같은 파일을 고치는 것이다. 아래 표 밖의 파일은 건드리지 않는다.

**소유 단위는 폴더다.** 파일 단위로 나누면 권한 규칙으로 막을 수가 없어서, 팀 하네스 기준(특강 day2 오전 「팀 하네스: 소유권과 교환」)에 맞춰 레인마다 폴더 하나를 준다.

| 폴더 | 레인 | 안에 있는 것 |
|---|---|---|
| `screen/` | **A** | `bank-ui.html`, `styles.css`, `CLAUDE.md` |
| `mission/` | **B** | `missions.json`, `mission-engine.js`, `quiz-data.json`, `quiz.js`, `CLAUDE.md` |
| `guide/` | **C** | `qa-data.json`, `ai-guide.js`, `voice.js`, `CLAUDE.md` |
| `shared/` | 통합자 | `CONTRACT.md`, `llm-config.js`(커밋 안 함), `CLAUDE.md` |

폴더 소유권의 부수 효과 셋:

1. **권한으로 막힌다.** 각자 `.claude/settings.json` 에 `Edit(mission/**)` 같은 규칙 세 줄이면 자기 폴더 밖 수정이 차단된다. 파일 단위였다면 파일이 늘 때마다 규칙을 고쳐야 한다
2. **`내폴더/CLAUDE.md` 가 생긴다.** 그 폴더 파일을 읽을 때만 자동으로 함께 읽히므로, 내 규칙이 팀원에게 걸리지 않는다
3. **뚫었을 때 추적된다.** `.claude/hooks/owner-guard.py` 가 폴더 이름으로 판단해 `CHANGELOG-INBOX/` 에 쪽지를 남긴다

담당자 이름·통합자·교환 규칙은 `OWNERS.md` 와 `ZIP-PROTOCOL.md` 에 있다. **담당은 아직 미정이다.**

| 파일 | 소유자 | 내용 |
|---|---|---|
| `screen/bank-ui.html` | **A** | 화면 7개 DOM, `BankUI`, 클릭 위임 배선, 도움바 DOM, `<script src>` 선언 |
| `screen/styles.css` | **A** | 전부 |
| `mission/mission-engine.js` | **B** | `MissionEngine` |
| `mission/missions.json` | **B** | 11.1에서 셋이 확정한 뒤 B만 |
| `mission/quiz.js` | **B** | `Quiz` (F16, Should) |
| `mission/quiz-data.json` | **B** | 퀴즈 문항 (F16, Should) |
| `guide/ai-guide.js` | **C** | `AIGuide` + 도움바 배선 |
| `guide/qa-data.json` | **C** | 미리 쓴 답변 |
| `guide/voice.js` | **C** | `Voice` (F12, Should) |
| `shared/CONTRACT.md` | 공동 | 11.1에서만. 이후 변경은 셋이 합의 |

**A가 `<script src>` 를 처음에 다 선언한다.** 파일이 아직 없으면 브라우저 콘솔에 404가 찍히지만 페이지는 정상 동작한다. 이렇게 하면 B와 C가 자기 파일을 추가하려고 `bank-ui.html` 을 열 이유가 사라진다.

**경로는 `../폴더명/파일` 이다.** 진입 페이지가 `screen/bank-ui.html` 이라 상대 경로의 기준이 `screen/` 이다. `fetch` 도 같은 기준이므로 `fetch('missions.json')` 은 404 이고 `fetch('../mission/missions.json')` 이 맞다. 폴더를 나눈 대가로 생긴 유일한 함정이다.

로드 순서는 고정이다. 뒤 파일이 앞 파일의 전역을 쓴다.

```
0. ../shared/llm-config.js             — window.LLM_CONFIG (없으면 404. LLM 미연결이 기본)
1. bank-ui.html 안의 인라인 <script>   — BankUI 정의 + 클릭 위임
2. ../mission/mission-engine.js        — MissionEngine
3. ../guide/voice.js                   — Voice
4. ../guide/ai-guide.js                — AIGuide + 도움바 배선 (BankUI·MissionEngine·Voice 를 쓴다)
5. ../mission/quiz.js                  — Quiz (BankUI 를 쓴다)
```

### 11.3 스텁 — 아무도 기다리지 않게

11.1 직후, **`mission-engine.js` · `ai-guide.js` · `voice.js` 의 스텁을 셋이 같이 만든다.** 계약에 적힌 함수만 있고 내용은 가짜인 파일이다.

| 스텁 | 하는 일 | 이걸로 확인 가능해지는 것 |
|---|---|---|
| `MissionEngine` | 무엇을 눌러도 다음 단계로 넘김 | A가 첫 순간부터 화면 7개를 클릭으로 왕복 |
| `MissionEngine.getMission` | `label` 붙은 가짜 미션 반환 | **A가 F17 진행 표시를 B를 기다리지 않고 확인** |
| `AIGuide.askAI` | `(question, step, mission)` 3인자로 `"(스텁 답변) ..."` 즉시 반환 | A가 도움바와 답변 박스 표시 확인. 인자 수가 스텁에서부터 3개다 |
| `Voice.isSupported` | 항상 `false` | A가 마이크 미지원 → 텍스트 폴백 경로 확인 |

B와 C는 자기 스텁 파일의 **내용을 통째로 교체**하는 것으로 작업을 시작한다. 파일을 새로 만들지 않으므로 `<script src>` 도 그대로다.

이 스텁이 SPEC 11.4 조건 2("서로의 결과를 기다리지 않는다")를 문서상 약속에서 실제 사실로 바꿔 준다.

### 11.4 담당

| 담당 | Must | Should / Could | 건드리지 말 것 |
|---|---|---|---|
| **A** | F3 홈화면, F4+F15 송금 6단계+완료화면(기본 `input` 포함), F8의 화면 쪽(흔들림·`showError`), F14의 DOM(도움바·텍스트 입력칸) | F1 폰셸, F5 키패드, F2 용어 병기 | `mission-engine.js`, `ai-guide.js`, `voice.js`, `quiz.js`, `*.json` |
| **B** | F6 `missions.json`, F7 상태머신, F8의 문구 쪽(`wrong` 메시지) | F16 금융용어 미니 퀴즈 (`quiz.js`, `quiz-data.json`) | `bank-ui.html`, `styles.css`, `ai-guide.js`, `voice.js`, `qa-data.json` |
| **C** | F13 `askAI` + `qa-data.json`, F14의 배선(도움바 클릭 → askAI → 화면 표시) | F12 마이크 STT (`voice.js`) | `bank-ui.html`, `styles.css`, `mission-engine.js`, `missions.json`, `quiz.js` |

F8이 A와 B로 갈린 이유: 흔들림과 표시는 A의 파일(`bank-ui.html`·`styles.css`)에 있고, "왜 틀렸는지"의 문장은 B의 파일(`missions.json`)에 있다. 파일 소유권을 따르면 자동으로 이렇게 나뉜다.

C의 도움바 배선이 A의 파일을 안 건드리는 방법: **A는 DOM만 놓고, C는 `ai-guide.js` 안에서 그 id를 찾아 스스로 이벤트를 붙인다.** `ai-guide.js` 는 `</body>` 앞에서 로드되므로 DOM이 이미 있다.

### 11.5 착수·통합 순서

```
S0  11.1 계약 + 11.3 스텁 + 서버 + git          (셋이 같이, 30분)
     |
     +--- A 레인 ---> 화면 7개 --> F3 --> F4+F15 --> 배선 --> F8 화면
     +--- B 레인 ---> missions.json --> F7 상태머신 --> F8 문구
     +--- C 레인 ---> qa-data.json --> F13 askAI --> F14 배선
     |
S1  합류 — 스텁이 전부 교체됐는지 확인 + 마우스로 완주
     |
S2  Must 합격 판정 — 6.1의 기준 1~6
     |
     +--- A: F1 폰셸 --> F5 키패드 --> F2 용어병기
     +--- B: F16 퀴즈
     +--- C: F12 마이크 STT
     |
S3  시연 리허설 — 마이크 차단·네트워크 차단 상태에서도 완주되는지
```

- **S1 이전에는 서로를 부르지 않는다.** 스텁이 있으므로 각자 자기 레인을 끝까지 갈 수 있다.
- **S2를 통과하지 못하면 Should로 넘어가지 않는다.** MVP는 Must 7항목이다.
- Should 착수 순서는 각 레인 안에서 위 순서를 따른다. 레인끼리는 다시 병렬이다.

### 11.6 병렬 성립 조건 점검

| 조건 | 상태 |
|---|---|
| 1. 파일이 겹치지 않는다 | ✅ 11.2 소유권 표. 표 밖 파일 금지 |
| 2. 서로의 결과를 기다리지 않는다 | ✅ 11.3 스텁. S0 직후부터 각자 동작하는 페이지를 본다 |
| 3. 같은 데이터 모양을 동시에 바꾸지 않는다 | ✅ `missions.json` 을 11.1에서 확정하고 이후 B만 수정. 1차 분류에서 구두 조율로 밀려났던 것이 파일 하나로 돌아왔다 |

**남은 조율 지점 두 개.** 이건 병렬로 안 되므로 말로 해야 한다.

- A가 화면을 늘리거나 `data-action` 값을 바꾸면 `missions.json` 의 `action` 과 어긋난다. → 바꾸기 전에 B에게 말한다.
- C가 `qa-data.json` 의 답변에서 "○○ 버튼을 누르세요"라고 쓸 때, 그 버튼 글자가 A의 화면과 달라지면 안 된다. → S1에서 대조한다.

### 11.7 충돌 났을 때

세 사람이 다른 파일만 고치면 git merge 충돌은 원리상 나지 않는다. 충돌이 났다면 누군가 남의 파일을 고친 것이다.

`.claude/agents/code-reviewer.md` 서브에이전트가 이걸 검사 항목으로 갖고 있다(담당 파일 침범). 커밋 전에 한 번 돌리면 사람이 눈으로 찾기 전에 잡힌다.

> `[확인 필요]` Q3에서 외부 LLM 실연결을 택하면 C의 `askAI` 안쪽만 바뀐다. 담당 재분배는 필요 없다.

---

## 12. 팀 결정 대기 (5건)

**팀이 정해야 하는 것**이라 한쪽으로 밀지 않았다. 각 항목의 "지금 값"이 문서·계획에 들어가 있으므로, 뒤집기로 결정하면 적힌 곳만 고치면 된다. 12.3~12.5 는 13절(SOL 레퍼런스 대조)에서 나온 것이다.

### 12.1 F11 음성 낭독(TTS) — Won't 유지인가, Should로 올리는가

| | 내용 |
|---|---|
| 지금 값 | **Won't** (10절 팀 1차·2차 확정). 코드에 `speechSynthesis` 호출이 없다 |
| 다른 의견 | 이전 논의에서 **Should 권장**이 나왔다. 반영하지 않은 채 남아 있다 |
| Should로 올리면 얻는 것 | MVP 밖이긴 하지만 Should까지 가면 **음성이 하나라도 생긴다.** 리스크 4(음성이 하나도 없다)가 실질적으로 줄고, 발표에서 "고령자용"이라는 주장의 근거가 는다. 글씨를 못 읽는 사용자에게는 F1(큰 글씨)보다 이게 더 크다 |
| 비용 | 브라우저 내장 `speechSynthesis` 라 API 키·네트워크가 필요 없다. `showAnswer`·`showError` 가 텍스트를 띄우는 지점에서 같이 읽으면 되므로 **30줄 안쪽**. iOS에서도 동작한다(5.2) |
| 위험 | 발표장에서 소리가 겹치거나 중간에 끊는 조작이 필요해진다. 끄는 버튼이 없으면 시연 중 방해가 된다 |
| 올리기로 하면 바꿀 곳 | ① 4.2 Won't 표에서 F11 제거, 4.1 Should에 추가 ② 10절 Should 표 ③ 담당은 **C**(답변 출력 경로를 이미 쥐고 있다) ④ **`.claude/agents/code-reviewer.md` 의 검토 항목 4번** — 지금은 `speechSynthesis` 호출을 무조건 지적하게 돼 있어서 그대로 두면 리뷰어가 정상 코드를 계속 문제로 올린다 ⑤ `plan.md` 의 Global Constraints |

### 12.2 F16 퀴즈 오답 처리 — 설명 후 다음인가, 다시 선택인가

| | 내용 |
|---|---|
| 지금 값 | **설명 후 다음 문제.** 다시 고를 기회 없음 (4.1 F16 상세) |
| 근거 | F9(반복 오답 힌트 상승)가 Won't인 것과 같은 방향. 퀴즈가 3~5문제뿐이라 재시도가 없어도 짧다 |
| 다시 선택으로 뒤집으면 | 정답을 맞힐 때까지 진행이 안 되므로 학습 효과는 올라가지만, 고령자가 같은 문제에서 갇힐 수 있다. 이때는 **몇 번 틀리면 정답을 알려주고 넘기는 규칙**이 하나 더 필요하다 — 그게 사실상 F9다 |
| 뒤집기로 하면 바꿀 곳 | `quiz.js` 의 `answer()` 하나. 화면·데이터는 그대로다. 담당은 **B** |

### 12.3 화면에 쓰는 은행명 — 실존 은행명인가, 가상 은행명인가

| | 내용 |
|---|---|
| 지금 값 | **실존 은행명.** `missions.json` 의 `value: "국민은행"`, 홈 출금계좌 `국민은행 123456-78-901234`, 은행 선택 버튼 `국민은행`·`신한은행`·`농협은행` |
| 왜 문제인가 | 레퍼런스 사용 조건이 **"브랜드명을 그대로 복제하지 않고 가상의 은행 UI로 제작"** 이다(13.1). 특히 `신한은행` 은 레퍼런스로 삼은 그 은행의 이름이다. 발표 자료에 실존 은행명이 박힌 가짜 뱅킹 화면이 뜨는 것도 부담이다 |
| 바꾸면 좋은 점 | 브랜드 문제가 사라진다. 미션 데이터로만 바뀌는 값이라 코드 수정이 없다 |
| 바꾸면 생기는 일 | `missions.json` 의 `value` 는 **공통 약속**이다(11.1). 셋이 같이 정한 값이라 임의로 못 바꾼다. A의 버튼 `data-value` 와 B의 `value` 와 C의 답변 문구 세 곳이 같이 바뀐다 |
| 제안(확정 아님) | 가상 은행명 3개로 교체 — 예: `한걸음은행` / `새벽은행` / `푸른은행`. 출금계좌도 같이 바꾼다. 형태(6자리-2자리-6자리)는 그대로 두면 실물 감각이 남는다 |
| 바꾸기로 하면 고칠 곳 | ① `mission/missions.json` 의 `value` ② `screen/bank-ui.html` 의 `data-value` 와 홈·확인 화면 라벨 ③ `guide/qa-data.json` 의 답변 문구 ④ `OWNERS.md` 「공통 약속」 표 ⑤ `plan.md` A2·A3·B1 ⑥ 13.3 T1 |

### 12.4 송금 비밀번호 6자리 단계를 넣을지

> **2026-08-19 정정(13.5):** 처음에는 "SOL 04가 비밀번호 화면"이라고 적었는데 **틀렸다.** 화면으로 확인하니 SOL의 **쉬운이체 모드는 비밀번호를 받지 않고 `이체하기` 한 번으로 완료**된다("이체가 바로 완료됩니다"). 즉 **레퍼런스를 따르면 넣지 않는 쪽**이다. 아래 표의 "얻는 것"은 일반 이체 경로를 기준으로 한 이야기로 남겨 둔다.

| | 내용 |
|---|---|
| 지금 값 | **없음.** 우리 6단계에 인증 단계가 없다 — **레퍼런스(쉬운이체)와 같다** |
| 넣으면 얻는 것 | 일반 이체 경로와 흐름이 같아진다. 고령자가 실제로 막히는 지점 하나가 인증이다 |
| 비용 | 단계가 7개가 되고 F17 분모가 5→6 으로 바뀐다. `missions.json` 에 단계 하나와 정답값이 추가되고, 6.1 기준 1·4·5 판정이 전부 한 단계 늘어난다. 키패드(F5)가 Should 인데 비밀번호는 키패드가 사실상 필수라 **Should 의존이 생긴다** |
| 위험 (이게 크다) | 학습용 화면에서 "비밀번호를 여기에 누르세요" 를 연습시키는 것은 **가짜 화면에 비밀번호를 넣는 습관**을 만들 수 있다. 보이스피싱 예방과 정면으로 어긋날 소지가 있다. 가짜 은행이라는 표시가 아무리 있어도 이 부분은 조심할 값어치가 있다 |
| 내 의견 | **넣지 않는 쪽.** 다만 이유가 범위가 아니라 위험이므로 팀이 알고 정하는 게 맞다 |
| 넣기로 하면 고칠 곳 | `missions.json`(단계 추가), `screen/bank-ui.html`(화면 1개), 4.1 F4+F15 설명, 6.1 기준, F17 분모, `plan.md` A3·B1 |

### 12.5 목표 금액을 화면에 알려줄지 (SOL 03)

| | 내용 |
|---|---|
| 지금 값 | **화면에 없다.** 금액 화면은 "얼마를 보내나요? / 수취인: 김민수 / placeholder 숫자만 입력" 이고, 5만원이라는 목표는 **옆 사람이 말로 알려주는 것**을 전제한다(S2 기준 1의 "김민수에게 5만원을 보내 보세요") |
| SOL | 입력칸 라벨에 **"50,000원을 입력해 주세요"** 라고 목표를 직접 적어 준다 (체험 가이드라서) |
| 넣으면 얻는 것 | 혼자 열어도 완주할 수 있다. 지금은 사람이 없으면 금액을 알 수 없어서, 6.1 기준 1(설명 없이 완주)이 **사실상 구두 지시에 의존**한다 |
| 넣으면 걸리는 것 | "○○을 입력해 주세요" 는 **지시**다. F10(단계별 안내 배너)이 Won't 인데 그 경계에 닿는다. F17(진행 표시)이 위치만 말하도록 선을 그은 것과 같은 문제다 |
| 선택지 | (가) 그대로 둔다 — 미션은 사람이 말로 준다 (나) 미션 제목(`김민수에게 5만원 보내기`)을 **홈 화면에 한 번만** 띄운다. 각 단계에는 안 띄우므로 F10과 구분된다 (다) SOL처럼 각 입력 화면 라벨에 목표를 적는다 — 사실상 F10 이므로 Won't 를 뒤집는 결정이 된다 |
| 내 의견 | **(나).** `missions.json` 의 `title` 이 이미 있어서 새 데이터가 필요 없고, 홈 한 곳이라 지시 배너가 되지 않는다 |
| 정하면 고칠 곳 | (나)를 택하면 `screen/bank-ui.html` 의 `#screen-home` 한 줄 + 4.1 F3 설명. (다)를 택하면 10절 Won't 표부터 고쳐야 한다 |

다섯 항목 다 **Must 기능의 구현을 막지 않는다.** 12.1·12.2 는 S2(Must 합격 판정) 이후에 정해도 되고, **12.3 은 A·B·C 가 화면과 데이터를 쓰기 시작하기 전(S0)에 정하는 것이 싸다** — 나중에 정하면 세 폴더를 같이 고쳐야 한다.

---

## 13. UI/UX 레퍼런스 — 신한 SOL뱅크 「돈 보내기」 체험

### 13.1 출처와 사용 범위

| 항목 | 값 |
|---|---|
| 출처 | `https://nsol.shinhan.com/contents/guide/step05__send-money/` (공식 체험 가이드, 8화면) |
| 무엇을 참고하나 | **단계 흐름 · 정보 구조 · 입력 방식 · 버튼 배치.** 고령자가 실제로 만나는 순서와 말투 |
| 무엇을 참고하지 않나 | **로고·브랜드명·고유 색상·이미지·아이콘.** 그대로 복제하지 않는다 (4.2) |
| 결과물 | 비슷한 **경험**을 주는 **가상 은행** 화면. 같은 화면을 베낀 것이 아니다 |

**이 절은 SPEC을 바꾸지 않는다.** SOL 흐름과 우리 SPEC이 다른 곳을 찾아 13.3에서 **반영 / 단순화 / 제외 / 팀 결정 필요** 로만 분류했다. 등급·값 변경은 팀이 한다.

### 13.2 SOL 실제 흐름 (브라우저로 8화면 직접 확인 · 2026-08-19)

화면이 전부 PNG여서 처음에는 텍스트만 긁었고, 그 결과 두 가지를 잘못 적었다. 브라우저로 열어 고쳤다 — 정정 내용은 13.5.

**모든 화면에 공통인 껍데기**

- 앱바: 왼쪽 `‹` + 화면 이름(`쉬운이체` / `이체완료` / `거래내역조회`), 오른쪽 아이콘 3개
  - 홈에서는 아이콘에 **글자 라벨이 붙는다**: `챗봇` · `음성` · `마이`. 이체 흐름에서는 아이콘만
- 화면 맨 아래 고정 문구: "따라하기 체험화면으로 실제 이체가 되지 않아요"
- 코치: 검은 말풍선 + 캐릭터, **빨간 점 `Click Here`** 가 다음에 누를 곳을 가리킨다

| # | 화면 | 본 것 |
|---|---|---|
| 01 | **쉬운 홈** | `쉬운 \| 홈` 토글. 계좌카드 = `입출금 쓸편한입출금통장` · `신한 110-123-456789` · **`623,540원`** + 새로고침 + 버튼 2개 `돈보내기`·`간편앱출금`. 아래 보안 배너, 그 아래 **2열 큰 타일 4개**: `전체계좌조회` `돈보내기` `상품가입` `ATM 돈찾기`. **진입점이 카드와 타일 두 곳에 있다** |
| 02 | **받는통장 입력 `1 / 3`** | 제목 왼쪽 `받는통장 입력`, **오른쪽에 `1 / 3` 배지**. 입력 블록 = 왼쪽 세로 라벨 `받는 통장` + 오른쪽 2행(`은행명 ▼` / `계좌입력`). 아래에서 **바텀시트** `받는통장 선택` 이 올라오고 탭 3개 `최근`·`내계좌`·`직접입력` + 은행 로고 2열 그리드 |
| 03 | **보내는 통장, 금액 입력 `2 / 3`** | 출금계좌 카드 = 통장 별칭 `신한 내 생활비 통장` · `신한 110-123-456789` · **`잔액 998,800원`**. 금액행 = 왼쪽 라벨 `금액`, `₩ … 원`, placeholder `(금 원)`. `다음` 버튼은 **처음에 비활성(회색)**. 입력칸을 누르면 하단에서 키패드가 올라온다 — **금액 칩 `1만` `5만` `10만` `100만` `전액`** 한 줄 + 숫자 3열(1~9) + `←` `0` `완료` |
| 04 | **입력사항 확인 `3 / 3`** | 카드 = `한행복님에게` / `신한 110123456789` / **`50,000 원`**(가장 큰 글씨) / `이체할까요?`. 아래 `받는분 메모  신건강 ▾`. 그리고 **확인 모달**: 로고 + `한행복님께` / `신한 110-123-123456` / `50,000원` / `보낼게요` + `✓ 내용을 확인했습니다.` + 버튼 `취소`(회색·왼쪽) `이체하기`(파랑·오른쪽), 툴팁 **"이체가 바로 완료됩니다."** — **비밀번호를 받지 않는다** |
| 05 | **이체완료** | 앱바 제목이 `이체완료` 로 바뀌고 **헤더 색이 파랑 → 초록**. 영수증 표 4행: `거래일시 2022.12.29 14:10:12` / `받는통장 신한 110-123-123456` / `금액 50,000원` / `받는분 메모 신건강`. 아래 `추가정보 ▼` 아코디언, "항상 신한은행을 이용해 주셔서 감사합니다." + 도장, 하단 공유(메시지카드·문자·스토리뱅크) |
| 06 | **거래내역조회** | 계좌 잔액이 **`948,800원`** — 998,800 에서 50,000 이 빠졌다. 목록에 `14:10:12 / 한행복 / 출금 50,000원` |
| 07 | **거래내역상세**(모달) | `메모입력(최대 20글자)` + 연필 아이콘. 표: `거래일시` / `거래구분 모바일` / `거래금액 50,000원` / `거래 후 잔액 948,800원`. `다시이체 ›` `이체결과내역 ›`. 공유 4개. 맨 아래 넓은 `확인` 버튼 |
| 08 | **미션 성공** | 화면을 어둡게 덮고 축하 캐릭터 + "따라하기 미션을 성공하셨습니다! 다음 따라하기를 이어해 주세요!" + **세로 버튼 3개**: `한번 더 따라하기 ↻`(흰) / `처음으로 이동하기 ⌂`(흰) / `ATM 돈 찾기 따라하기 →`(파랑·강조). 뒤 목록에는 07에서 넣은 메모 `손주용돈` 이 붙어 있다 |

우리 흐름(4.1 F4+F15): `home` → `bank` → `account` → `amount` → `confirm` → `done`.
SOL의 입력 단계는 **3개**(받는통장 / 보내는통장·금액 / 확인)이고 완료는 세지 않는다.

### 13.3 차이 분류

#### 반영 — SOL 방식이 맞고 우리도 그렇게 되어 있다(또는 근거가 보강됐다)

| # | 무엇 | 상태 |
|---|---|---|
| R1 | **홈에서 시작한다.** 앱을 열면 계좌 화면이고 거기서 이체를 누른다 | 이미 일치 — F3 → `transfer` |
| R2 | **금액 입력은 숫자 키패드로 한다.** SOL 03에는 키보드가 없고 키패드만 있다 | **F5(큰 숫자 키패드) 근거 보강.** 실제 앱에는 키패드가 유일한 입력 수단이므로, 기본 `<input>` 만 있는 Must 상태는 실제 앱과 다르다. 등급(Should)은 10절 확정본이라 그대로 두고 근거만 기록한다 |
| R3 | **계좌번호를 하이픈 포함 형태로 보여준다**(`110-123-123456`) | 이미 대응 — 판정은 `digits()` 로 숫자만 비교하므로 하이픈을 넣어도 통과한다 |
| R4 | **완료 화면에서 "한번 더" 로 돌아간다** (SOL 08 `한번 더 따라하기`) | 이미 일치 — F15 의 `restart`·"다시 연습하기" |
| R5 | **연습이고 실제 거래가 아니라고 명시한다** (SOL 00·"실제 거래가 되지 않아요") | 이미 일치 — 완료 화면의 "연습이라 실제 돈은 움직이지 않았습니다" |
| R6 | **말투가 짧은 존댓말 질문형**("~을 입력해 주세요") | 이미 일치 — 화면 제목이 "계좌번호를 넣으세요" 형태다 |
| R7 | **진행 표시가 실제로 있다** — 제목 오른쪽에 `1 / 3` `2 / 3` `3 / 3` 배지 | **F17의 근거가 바뀌었다.** 학습용으로 우리가 덧붙인 장치가 아니라 **실제 앱이 쓰는 패턴**이다. 등급(F4+F15 안의 Must 구성요소)은 그대로, 근거만 강해졌다. 위치도 같다 — 화면 제목 줄의 작은 배지 |
| R8 | **확인 화면에서 금액이 가장 큰 글씨다** (`50,000 원`) | 이미 일치 — 확인·완료 화면에서 금액만 `<strong>` 으로 키운다 |
| R9 | **도움 진입점이 앱바에 상시 있다** — `챗봇`·`음성` 아이콘, 홈에서는 **글자 라벨까지 붙는다** | **F14 배치 근거.** 우리 도움바도 어느 화면에서나 같은 자리에 있고, 아이콘만 두지 말고 글자를 같이 쓴다. `음성`(F12)이 실제 앱에서도 챗봇과 나란히 있다는 것도 확인됐다 |
| R10 | **다음 버튼이 입력 전에는 비활성(회색)** | 우리는 항상 활성이고 틀리면 F8이 설명한다. **의도한 차이** — 눌러서 틀려 보는 것이 이 서비스의 학습 방식이다(SPEC 3절). 비활성은 "왜 안 눌리지" 로 막히게 만든다 |
| R11 | **완료 화면에서 헤더 색이 파랑 → 초록으로 바뀐다** | 값싼 반영 후보. 상태가 끝났음을 색으로 한 번 더 알린다. A의 `styles.css` 에서 `:root` 토큰 하나 추가로 끝난다(다크모드 오버라이드 같이) — 등급 변경이 아니라 F15 안의 표현이므로 A 재량 |

#### 단순화 — SOL과 다르게 가되 학습 목적상 이유가 있다

| # | SOL | 우리 | 왜 |
|---|---|---|---|
| S1 | 은행 선택과 계좌번호 입력이 **한 화면**(02) | **두 화면**(`bank` → `account`) | 한 화면에 한 결정. 고령자가 처음 배울 때는 화면당 할 일이 하나여야 오답 지점을 특정할 수 있다. F8(오답 피드백)도 단계별로 문구가 달라야 성립한다 |
| S2 | 확인 단계가 곧 비밀번호 입력(04)으로 이어진다 | `confirm` 화면에서 항목을 보여주고 `보내기` 하나 | 인증을 뺐기 때문에(T2 참조) 확인 화면이 "마지막으로 눈으로 검토하는 자리" 역할만 한다 |
| S3 | 진행 표시 `1 / 3` — **입력 단계만 세고 완료는 안 센다** | **F17 `n / 5`** — 완료까지 센다 | ~~실제 앱에 없는 학습용 장치~~ → **정정: SOL에도 있다**(R7). 다른 것은 분모다. SOL은 입력 3단계, 우리는 `bank`~`done` 5단계. 학습에서는 "완료까지 몇 걸음 남았나"가 동기이므로 완료를 센다. 바꾸려면 `missions.json` 의 `label` 에서 `done` 을 비우면 되고 코드는 안 고친다 |
| S4 | 은행 선택이 **바텀시트 + 탭 3개**(`최근`·`내계좌`·`직접입력`) | **화면 하나**(`bank`) | SOL도 은행 선택을 별도 층으로 분리한다 — 한 화면에 다 있는 것처럼 보였지만 실제로는 시트가 덮는다. 우리의 화면 분리가 SOL과 크게 다르지 않다는 근거. 시트·탭·최근계좌는 1일 범위 밖이라 화면 하나로 대신한다 |
| S5 | 확인 모달에 `취소` / `이체하기` 두 버튼 + `✓ 내용을 확인했습니다` | `보내기` 하나 | 취소는 "이전 단계로 되돌리기" 이고, `MissionEngine.submit` 계약에 **뒤로 가기 개념이 없다**(F7). 넣으면 상태머신이 커진다. 대신 오답으로 막혔을 때 F8이 설명하는 쪽으로 간다 |

#### 제외 — 이번 MVP에 넣지 않는다

| # | SOL | 왜 빼나 |
|---|---|---|
| X1 | **메모 입력**(06·07) | 송금을 익히는 것과 무관. 3명·1일 범위 밖 |
| X2 | **다음 미션으로 이어가기**(08 `ATM 돈 찾기 따라하기`) | 미션은 송금 1개다(4.3). 이어갈 다음 미션이 없다 |
| X3 | **수수료 표시** | 가짜 은행이라 수수료 개념이 학습 목표에 없다. 확인 화면은 출금계좌·수취인·계좌번호·금액만 |
| X4 | 좌측 전체메뉴·앱 다운로드·이용안내 등 **가이드 사이트 자체의 껍데기** | 우리는 앱 화면만 만든다 |
| X5 | **금액 빠른 선택 칩** `1만` `5만` `10만` `100만` `전액` (SOL 03 키패드 위) | 우리 미션 목표가 5만원이다. `5만` 칩을 두면 **한 번 눌러 정답이 되어 금액을 넣는 연습 자체가 사라진다.** 실제 앱에는 있는 편의 기능이지만 학습용에서는 빼는 것이 맞다 |
| X6 | **이체 후 잔액이 줄어든다** (998,800 → 948,800), 거래내역에 건이 쌓인다 | 잔액·내역을 상태로 들고 있어야 하는데 "저장 없음"이 이 MVP의 전제다(F16도 점수 저장이 없다). 완료 화면 문구로 대신한다. 필요하다고 판단되면 12절에 결정 항목으로 올린다 |
| X7 | **거래내역상세**(거래구분·거래 후 잔액·다시이체·공유 4종) | 미션이 송금 1개다. 거래내역 미션 자체가 4.3에서 제외다 |
| X8 | 캐릭터 코치 말풍선 + **빨간 점 `Click Here`** 로 다음 누를 곳을 찍어 주는 방식 | 이게 **F10(지시 배너)의 정체**다. SOL은 "체험 가이드"라서 다음 누를 곳을 찍어 주지만, 우리는 **스스로 찾아 누르는 것**을 학습 목표로 삼았다(SPEC 1절: 대신 해주지 않는다). 넣으면 F10 Won't 를 뒤집는 결정이 된다 |

#### 팀 결정 필요 — 내가 정하지 않았다 (12절에 옮겨 적었다)

| # | 무엇 | 12절 |
|---|---|---|
| T1 | 화면에 쓰는 **은행명**. 현재 계획에 `국민은행`·`신한은행`·`농협은행` 이 그대로 있다 — 실존 브랜드명이고, 특히 `신한은행` 은 레퍼런스 은행 이름이다 | **12.3** |
| T2 | **6자리 비밀번호 입력 단계**(SOL 04)를 넣을지 | **12.4** |
| T3 | **목표 금액을 화면에 알려줄지**(SOL 03 "50,000원을 입력해 주세요") | **12.5** |

### 13.4 확인한 것과 아직 못 본 것

브라우저로 8화면(01~08)을 열어 13.2를 채웠다. 처음에 "못 읽었다"고 적었던 네 가지는 아래처럼 해결됐다.

| 처음에 못 읽었던 것 | 지금 |
|---|---|
| 05 화면의 정체 | **`이체완료` 영수증 화면.** 헤더가 초록으로 바뀌고 거래일시·받는통장·금액·메모 4행 |
| 확인 화면의 항목과 순서 | **`받는 사람 → 계좌번호 → 금액(대) → "이체할까요?"`**, 그 위에 진행 배지 `3 / 3`, 아래 `받는분 메모`. 그리고 같은 내용을 한 번 더 보여주는 확인 모달 |
| 버튼 위치·크기·색 | 주 버튼은 **화면 폭을 꽉 채우고 아래쪽**, 파란 단색. 보조는 회색·왼쪽(모달) 또는 흰 배경(미션 성공). 키패드는 **하단에서 올라오고 3열** |
| 예외 화면 | **이 체험에는 없다.** 정해진 한 경로만 따라가게 되어 있어 오답·잔액 부족 화면이 존재하지 않는다 |

**마지막 항목이 우리에게 중요하다.** SOL 체험은 빨간 점이 찍어 주는 대로만 누르므로 **틀릴 수가 없다.** 즉 오답 피드백(F8)은 SOL에서 베낄 대상이 없고, **우리가 직접 설계해야 하는 부분**이다. 이 서비스가 SOL 체험과 다른 지점이 정확히 거기다.

아직 안 본 것(필요해지면 그때 본다):

- `step04__search-account`(계좌조회)·`step06__withdrawal-from-atm`(ATM) 등 **다른 미션의 흐름** — 이번 MVP는 송금 1개라(4.3) 볼 이유가 없다
- 일반 이체(쉬운이체가 아닌 경로)의 비밀번호·인증 화면 — 13.5 정정 2 참조
- 다크모드·글씨 크기 확대 시의 레이아웃

### 13.5 처음 기록에서 정정한 것 두 건

텍스트만 긁었을 때 잘못 적었던 것이다. **결론이 바뀌므로 남겨 둔다.**

| # | 처음에 적은 것 | 실제 | 무엇이 바뀌나 |
|---|---|---|---|
| 1 | "SOL 실제 앱에는 진행 표시가 없다. F17은 학습용 장치" | 제목 오른쪽에 **`1 / 3` `2 / 3` `3 / 3` 배지가 있다** | F17 이 "우리가 덧붙인 것"에서 **"실제 앱 관행을 따른 것"** 으로 바뀐다. 13.3 R7·S3 갱신 |
| 2 | "SOL 04는 6자리 비밀번호 입력 단계" | **비밀번호 단계가 없다.** 04는 `입력사항 확인 3 / 3` 이고, 확인 모달의 `이체하기` 옆 툴팁이 **"이체가 바로 완료됩니다."** 다. `6자리 비밀번호를 입력해 주세요.` 라는 문구는 04.html 의 코치 말풍선 DOM(`heading`)에는 남아 있으나 **화면에 렌더되지 않는다** — 쉬운이체 모드는 비밀번호를 받지 않는다 | **12.4의 전제가 뒤집힌다.** "실제 앱에 있으니 넣을까"가 아니라 **"레퍼런스인 쉬운이체도 안 받는다"** 가 된다. 12.4 갱신 |
