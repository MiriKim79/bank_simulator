# 합치기 규칙 — Git 사용 (ZIP 교환은 예비)

> ## 이 팀은 Git 을 쓴다 (2026-08-19 확정)
>
> 원격: `https://github.com/MiriKim79/bank_simulator.git` — 브랜치 `main` / `develop`, PR·이슈 템플릿 있음.
> **아래 ZIP 교환 규칙은 예비다.** 인터넷·계정 문제로 Git 을 못 쓰는 사람이 생겼을 때만 쓴다.
>
> **Git 으로 바뀌어도 살아 있는 규칙은 하나다 — 규칙 4(공통 약속은 고치기 전에 먼저 알린다).**
> 함수 이름·데이터 모양·DOM id 는 Git 이 막아 주지 못한다. merge 는 조용히 성공하고 화면만 죽는다.

## Git 협업 규칙

| 항목 | 값 |
|---|---|
| 브랜치 | `feature/*`(기능 개발, 담당자별) → `develop`(통합·검증) → `main`(배포) |
| 작업 방식 | **각자 자기 `feature/*` 브랜치에서 커밋하고, PR을 `develop` 대상으로 연다.** `main`·`develop` 직접 커밋은 원칙적으로 금지 — 공통 계약(`shared/CONTRACT.md`) 변경처럼 셋이 같이 고쳐야 하는 경우만 합의 후 예외로 `develop`에 바로 커밋한다 |
| `main` | `develop` 을 **머지해서만** 올라간다. 직접 커밋하지 않는다 |
| 커밋 | `feat(a1): ...` 처럼 레인·Task 번호를 앞에 붙인다. `plan.md` 각 Task 의 커밋 메시지를 그대로 쓴다 |
| 올리기 전 | **반드시 `git pull` 먼저.** 셋이 같은 브랜치에 올리므로 남의 커밋 위에 얹어야 한다 |
| 남의 폴더 | 자기 폴더 밖 파일이 diff 에 있으면 안 된다 (`.claude/agents/code-reviewer.md` 검토 항목 3). 커밋 전에 `git status` 로 확인 |
| 검증 | `OWNERS.md` 의 검증 3단계를 통과한 것만 푸시한다 — **깨진 `develop` 은 세 사람을 동시에 막는다** |

**feature 브랜치 → `develop` PR 이 기본 흐름이다.** 테스트 러너가 없으니 리뷰를 대신할 두 가지를 지킨다.

1. **PR 올리기 전에 `develop` 을 내 `feature/*` 에 `merge`(또는 `rebase`).** 오래된 브랜치로 PR을 열면 충돌이 늦게 발견된다
2. **PR 병합 전에 검증 3단계.** `OWNERS.md` 의 검증 3단계를 통과한 것만 `develop`에 머지한다

`.github/pull_request_template.md` 와 이슈 템플릿은 원격에 이미 있다. `feature/* → develop` PR에 그대로 쓴다.

`.claude/` 는 **커밋한다**(팀 하네스 공유). 단 `.claude/settings.local.json` 은 `.gitignore` 에 있다 — 권한 규칙은 사람마다 다르다.

폴더 소유권이 지켜지면 **merge 충돌은 원리상 나지 않는다.** 같은 브랜치를 써도 서로 다른 파일을 고치기 때문이다. 충돌이 났다면 누군가 남의 폴더를 고친 것이다.

## `main` 으로 내보내기 (통합자)

`develop` 이 검증 3단계를 통과한 상태일 때만.

```bash
git checkout main
git pull
git merge develop
git push
git checkout develop      # 바로 develop 으로 돌아온다
```

`main` 에 올린 뒤 `SYNC-LOG.md` 에 한 줄 남긴다.

---

## 예비 — Git 을 못 쓸 때의 ZIP 교환

카톡으로 파일을 주고받으며 협업하는 방법. **전제:** `OWNERS.md` 대로 폴더를 한 사람씩 독점하고 있어야 한다.

## 규칙 5개

```
규칙 1  보낼 땐 내 폴더 ZIP 하나만. 프로젝트 전체를 보내지 않는다.

규칙 2  합치는 사람은 통합자 1명. 나머지는 절대 안 합친다.

규칙 3  받은 배포판은 [내 폴더만 빼고] 덮어쓴다.
        내 폴더는 내가 유일한 소유자다 → 내 컴퓨터 것이 언제나 최신.

규칙 4  공통 약속(함수 이름·데이터 모양·DOM id)을 바꿔야 하면
        고치기 전에 카톡. OWNERS.md 갱신은 통합자가 한다.

규칙 5  파일 이름에 시각을 박는다.
        screen_김영원_0819-1150.zip
```

우리 프로젝트에서 규칙 4에 걸리는 것 세 개는 `CLAUDE.md` 「공통 약속」에 적어 뒀다 —
`askAI` 인자 3개, `missions.json` 의 `label`, 버튼 `data-action` 값.

## 보낼 때 (팀원)

```
내 담당 폴더만 압축해서 screen_김영원_0819-1150.zip 으로 만들어줘.
```

카톡 문구: `[보냄] screen  v7기준  김영원`
`v7기준` = 내가 받아서 작업한 배포판 번호(`VERSION.txt`).

`CHANGELOG-INBOX/` 에 내가 쓴 쪽지가 있으면 같이 넣는다.

## 받을 때 (팀원)

배포판 ZIP 이 카톡에 뜨면 **아무 때나** 받아도 된다.

```
받은 zip 을 풀어서 screen/ 폴더만 빼고 나머지를 내 프로젝트에 덮어써줘.
그리고 VERSION.txt 는 새 걸로 바꿔줘.
```

> 이 작업은 `Bash` 로 파일을 갈아끼우는 것이라 `Edit` 의 `deny` 에 안 걸린다. **의도된 설계다** —
> 규칙은 "손으로 고치는 것"을 막고, "받아서 갈아끼우는 것"은 다른 일이다.

받은 뒤 `CHANGELOG-INBOX/` 에 **내 폴더 관련 쪽지**가 있으면 읽는다.

## 통합자 루프 (5분)

```
카톡에 ZIP 뜸
  ↓
받은 폴더 통째 교체
  ↓
OWNERS.md 의 검증 3단계 실행
  ↓
실패 → 담당자 카톡 호출. 배포하지 않는다
  ↓
통과 → SYNC-LOG.md 한 줄 + VERSION.txt 올림
  ↓
전체 ZIP 배포: 금융한걸음_v8_0819-1220.zip  (personal/ 은 빼고)
```

카톡 문구: `[배포] v8  검증통과  screen(A) mission(B) 반영`

## 자주 나는 사고와 처방

| 사고 | 왜 났나 | 처방 |
|------|---------|------|
| 내 작업이 사라졌다 | 배포판을 **통째로** 덮어썼다 | 규칙 3 — 내 폴더는 빼고 덮어쓴다 |
| 합쳤더니 안 돈다 | 공통 약속을 말없이 바꿨다 | 규칙 4 — 고치기 전에 카톡 |
| `fetch` 가 404 난다 | 경로 기준이 열려 있는 페이지(`screen/`)다 | `../mission/missions.json` 처럼 `../` 를 붙인다 |
| 누가 최신인지 모른다 | 파일 이름에 시각이 없다 | 규칙 5 + `VERSION.txt` |
| 두 사람이 같은 파일을 고쳤다 | 폴더 독점이 안 지켜졌다 | `OWNERS.md` 부터 다시 |
